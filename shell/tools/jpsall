#!/bin/bash
# =============================================================
# jpsall — 查看集群所有节点 Java 进程
# 用法: jpsall
# =============================================================

SERVERS=(hadoop101 hadoop102 hadoop103)

for host in "${SERVERS[@]}"; do
    echo "===== $host ====="
    ssh "$host" jps
done
