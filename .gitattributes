# 强制 shell 脚本使用 LF 换行（Windows 检出时也保持 LF，防止在 Linux 上 CRLF 报错）
*.sh text eol=lf

# 无扩展名的 shell 脚本（如 shell/tools/xsync）
shell/tools/xsync text eol=lf
shell/tools/xcall text eol=lf
shell/tools/jpsall text eol=lf
